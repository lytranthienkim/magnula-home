export const DecreaseQuantityButton = ({ onClick }) => {
    return (
        <button className="px-2 py-0.5 body-03 text-display-regular" onClick={onClick}>
            -
        </button>
    );
};
